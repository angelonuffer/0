uniteste # ../uniteste.0

// Test if calculated properties work in lists
uniteste.descrever(["Lista com propriedade calculada" [
  uniteste.iguais([(
    chave = "nome"
    
    lista = [
      [chave]: "João"
      "valor_sem_chave"
    ]
    
    lista["nome"]
  ) "João"])
]])