uniteste = ../integração/uniteste.0

// Test URL import as value
// Note: Remote modules that use the old # syntax are no longer supported

uniteste.descrever({ nome: "Importação de URL como valor" testes: [
  // Test that we can import a local module using URL literal syntax
  uniteste.iguais([
    (
      uniteste_local = ../integração/uniteste.0
      // Just verify it's an object with expected properties
      (uniteste_local.iguais != 0) ? 1 : 0
    )
    1
  ])
]})
