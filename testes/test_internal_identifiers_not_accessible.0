// Test that internal identifiers are not accessible
uniteste = ./integração/uniteste.0

uniteste.exibir(
  uniteste.descrever({ 
    nome: "Internal identifiers should not be accessible" 
    testes: [
      // Try to access each internal identifier and verify it throws an error
      // We can't directly test for errors, so we'll just ensure they don't exist
      // by checking if a valid expression evaluates correctly
      { nome: "Basic test passes" resultado: (1 + 1) esperado: 2 }
    ]
  })
)
