// Test that multiple arguments are rejected
(
  soma = args => args[0] + args[1]
  
  // Try a call that should fail (multiple arguments)
  // soma(3, 7)  // This should cause a syntax error
  
  // This should work (single argument)
  resultado = soma([3, 7])
  resultado
)